
var
  MockApiClient = require('../spec/MockApiClient.js').MockApiClient;

describe('MockApiClient', function() {
  it('should', function() {
    var
      mac = MockApiClient();
      
//    mac.build_mocks();
  });
});